export const RightIcons = styled(MUIBox)(() => ({
  display: 'flex',
  gap: 8,
  paddingLeft: 6,
}));

export const RightIcon = styled(MUIBox)(() => ({
  display: 'flex',
  alignItems: 'center',
  cursor: 'pointer',
}));

export const ErrorMessage = styled('span')(() => ({
  display: 'block',
  marginTop: 3,
}));
